def main(A,B):
    rr=[]
    for x in range(B,A-1,-1):
        rr.append(x)
    """
    Return the numbers from B to A in the form of a list.
    Args:
        A: int
        B: int
    Returns:
        list: return  answer
    """
    return rr
