def main(n):
    rr='"'
    for x in range(n):
        rr +=str(x)
        rr +=','
    rr +='"'
    return rr
    """
    Return numbers from zero to n in a string view.
    Args:
        n: int
    Returns:
        string: return  answer
    """

