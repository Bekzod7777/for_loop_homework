import xdrlib


def main(A,B):
    rr=[]
    for x in range(A,B+1):
        rr.append(x)
    """
    Return the numbers from A to B in the form of a list.
    Args:
        A: int
        B: int
    Returns:
        list: return  answer
    """
    return  rr
