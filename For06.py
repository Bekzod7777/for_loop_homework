from re import S


def main(A,B):
    s=0
    for x in range(A,B):
        s +=x
    """
    Return the sum of all integers from A to B.
    Args:
        A: int
        B: int
    Returns:
        int: return  answer
    """
    return s

